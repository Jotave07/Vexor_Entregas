import { PageLoader } from "@/components/ui/page-loader";

export default function RootLoading() {
  return <PageLoader title="Abrindo VEXOR Entregas" message="Aguarde enquanto a plataforma prepara o portal logistico." />;
}
